# Directory: test

This directory contains test files for the Automated ICD Diagnosis Extraction Project.

## Files

- `test_config.py` - Basic configuration and directory structure tests

## Usage

Run the tests to verify that the project structure and configuration are set up correctly:

```bash
python -m pytest test/
```

Or run individual test files:

```bash
python test/test_config.py